;[readfile]
;cmdline = /usr/local/bin/readfile.sh
;interval = onchange
;watch = /var/tmp/date.txt
;run = periodic
;user = kain

;[readfile2]
;cmdline = /usr/local/bin/readfile.sh
;interval = stop-message
;run = periodic
;user = kain

